# mp4Label 视频标注工具 - 详细使用教程

## 目录

1. [工具简介](#工具简介)
2. [快速开始](#快速开始)
3. [详细使用步骤](#详细使用步骤)
4. [高级功能](#高级功能)
5. [只读查看模式](#只读查看模式)
6. [任务目录与任务组](#任务目录与任务组)
7. [常见问题](#常见问题)

---

## 工具简介

mp4Label 是一款专业的视频标注工具，专门用于标注视频剪辑教程。它可以帮助您：

- 📹 快速浏览和管理视频文件
- ⏱️ 精确标注教程的关键步骤时间点（毫秒级精度）
- ✏️ 编辑和整理教程步骤说明
- 💾 自动保存标注结果
- 🤖 对比模型标注与人工标注
- 👁️ 只读查看已完成的标注
- 📂 按任务组分组管理标注任务

### 主要特点

- ✅ **单文件运行**：只需一个可执行文件，无需安装其他依赖
- ✅ **跨平台支持**：支持 Windows、macOS、Linux
- ✅ **精确时间戳**：支持毫秒级精度的时间标注（mm:ss.SSS）
- ✅ **快捷键操作**：支持键盘快捷键，提高标注效率
- ✅ **预标注支持**：可以加载和编辑 AI 生成的预标注文件
- ✅ **自动保存**：编辑后 1.5 秒自动保存，切换视频时立即保存
- ✅ **模型对比**：可选的第四面板，对比模型标注与人工标注
- ✅ **原生对话框**：支持系统文件/文件夹选择对话框
- ✅ **只读模式**：`view` 子命令以只读方式查看标注，防止误修改
- ✅ **任务目录**：将多个任务文件放在一个目录中，通过下拉框切换任务组

---

## 快速开始

### 第一步：获取程序文件

根据您的操作系统，选择对应的可执行文件：

- **Windows 用户**：`mp4label.exe`
- **macOS 用户**：`mp4label` 或 `mp4label-darwin`
- **Linux 用户**：`mp4label-linux`

> 💡 **提示**：您只需要这一个文件即可运行，无需安装其他软件。

### 第二步：启动程序

#### Windows 用户

在命令行（cmd 或 PowerShell）中运行：
```bash
mp4label.exe web
```

#### macOS/Linux 用户

1. 打开终端（Terminal）
2. 进入程序所在目录
3. 运行以下命令：
   ```bash
   ./mp4label web
   ```
   
   > 💡 **提示**：如果是第一次运行，可能需要给程序添加执行权限：
   > ```bash
   > chmod +x mp4label
   > ```

#### 指定端口

如果默认的 8080 端口被占用，可以指定其他端口：
```bash
./mp4label web -port 3000
```

#### 指定任务目录启动

如果你有一个包含多个任务文件的目录，可以在启动时直接指定：
```bash
./mp4label web -task-dir /path/to/tasks
```

### 第三步：打开浏览器

程序启动后，您会看到类似以下信息：

```
mp4Label v0.2.9
Server started at http://localhost:8080
Mode: Edit (read-write)
Press Ctrl+C to stop
```

1. 打开您的浏览器（推荐使用 Chrome、Firefox 或 Edge）
2. 在地址栏输入：`http://localhost:8080`
3. 按回车键访问

> ⚠️ **注意**：请保持终端窗口打开，关闭终端窗口会停止程序运行。

---

## 详细使用步骤

### 步骤 1：首次配置

第一次使用时，需要配置必要的路径信息。

#### 1.1 打开配置界面

1. 在浏览器中打开 mp4Label 后，您会看到主界面
2. 点击右上角的 **"Config"** 按钮
3. 配置对话框会弹出

#### 1.2 设置视频目录

1. 在 **"Video Directory"**（视频目录）输入框中，输入您存放视频文件的文件夹路径
   - 例如：`C:\Videos\Tutorials`（Windows）
   - 例如：`/Users/username/Videos`（macOS）
   - 例如：`/home/username/videos`（Linux）
2. 也可以点击输入框右侧的 **📁** 按钮，打开系统文件夹选择对话框
3. 这个目录应该包含您要标注的 `.mp4` 视频文件

> 💡 **提示**：您可以直接从文件管理器复制文件夹路径，然后粘贴到输入框中。路径中的引号会被自动清理。

#### 1.3 设置输出目录

1. 在 **"Output Directory"**（输出目录）输入框中，输入您希望保存标注结果的文件夹路径
   - 例如：`C:\Videos\Annotations`（Windows）
   - 例如：`/Users/username/Annotations`（macOS）
2. 或点击 **📁** 按钮选择文件夹
3. 这个目录用于保存您标注完成后的文件

> ⚠️ **注意**：请确保输出目录存在，如果不存在请先创建该文件夹。

#### 1.4 设置预标注目录（可选）

如果您有 AI 生成的预标注文件，可以设置 **"Pre-annotation Directory"**（预标注目录）：

1. 在输入框中输入预标注文件所在的文件夹路径，或点击 **📁** 选择
2. 如果暂时没有预标注文件，可以留空

#### 1.5 设置任务文件（可选）

如果您只想标注特定的视频文件，可以设置 **"Task File"**（任务文件）：

1. 创建一个文本文件（`.txt` 格式）
2. 在文件中每行写一个视频文件名（不需要 `.mp4` 扩展名）
   ```
   video1
   video2
   tutorial_intro
   ```
3. 在配置对话框中输入这个文本文件的完整路径，或点击 **📄** 按钮选择文件

#### 1.6 设置任务目录（可选，推荐团队使用）

如果您有多个任务文件（例如每个标注员一个），可以设置 **"Task Directory"**（任务目录）：

1. 将所有 `.txt` 任务文件放在同一个目录下，例如：
   ```
   /tasks/
     ├── annotator_alice.txt
     ├── annotator_bob.txt
     └── annotator_carol.txt
   ```
2. 在配置对话框中输入这个目录的路径，或点击 **📁** 选择
3. 设置后，侧边栏会出现任务组下拉选择框

> ⚠️ **注意**：如果同时设置了"任务文件"和"任务目录"，**任务目录优先**，任务文件会被忽略。

#### 1.7 设置模型标注目录（可选，算法工程师使用）

如果您需要对比模型生成的标注，可以设置 **"Model Annotation Directory"**（模型标注目录）：

1. 在输入框中输入模型标注文件所在的文件夹路径，或点击 **📁** 选择
2. 设置后，界面右侧会多出一个"模型标注"面板

#### 1.8 保存配置

1. 检查所有路径是否正确
2. 点击 **"Save"**（保存）按钮
3. 配置会自动保存，下次启动程序时会自动加载

> 💡 **提示**：配置信息保存在 `~/.mp4label/config.json`，您可以随时修改。

---

### 步骤 2：选择视频

配置完成后，主界面会显示视频列表。

#### 2.1 选择任务组（如果配置了任务目录）

如果配置了任务目录，侧边栏顶部会出现一个**蓝色边框的下拉选择框**：

1. **默认选中第一个任务文件**，只显示该任务组中的视频
2. 点击下拉框可以切换到其他任务组
3. 选择 **"📋 All Videos"** 会显示所有视频（不做任何过滤）
4. 下拉框标签会实时显示当前视频数量，例如：`Task Group (42 videos):`

#### 2.2 查看视频列表

1. 左侧边栏会显示视频文件列表
2. 每个视频会显示：
   - **编号**：自动生成的序号（如 #0001）
   - **文件名**：视频文件的名称
   - **状态标识**：
     - 🟡 **Pre-annotated**：有预标注文件
     - 🟢 **Annotated**：已标注完成
     - 🔴 **None**：未标注

#### 2.3 统计信息

在搜索框下方会显示统计面板：

- **Total**：视频总数
- **Annotated**：已标注数量
- **Pre-annotated**：有预标注数量
- **Unannotated**：未标注数量

这些信息帮助您了解当前任务组的标注进度。

#### 2.4 搜索视频

如果视频很多，可以使用搜索功能：

1. 在视频列表上方的搜索框中输入关键词
2. 视频列表会实时过滤，只显示匹配的视频

#### 2.5 过滤视频

使用过滤按钮可以快速筛选视频：

- **All**：显示所有视频
- **None**：只显示未标注的视频
- **Pre-annotated**：只显示有预标注但未完成的视频
- **Annotated**：只显示已标注完成的视频

#### 2.6 选择视频

1. 在视频列表中点击您要标注的视频
2. 视频会在中间的播放器中加载
3. 右侧的标注编辑器会显示该视频的标注信息（如果有）

---

### 步骤 3：开始标注

#### 3.1 输入教程标题

1. 在右侧标注编辑器的 **"Tutorial Title"**（教程标题）输入框中
2. 输入这个教程的标题
   - 例如："电影滚动字幕制作教程"
   - 例如："高级感变速卡点效果"

#### 3.2 播放视频

1. 点击视频播放器中的播放按钮，或按键盘上的 **空格键** 开始播放
2. 观看视频内容，了解教程的整体流程

#### 3.3 插入时间戳

当视频播放到关键步骤时，有多种方式插入时间戳：

**方法一：使用快捷键（推荐）**

1. 当视频播放到关键步骤时，按键盘上的 **`I`** 键
2. 视频会自动暂停
3. 系统会自动创建一个新的步骤，并填入当前时间戳
4. 光标会自动定位到描述输入框
5. 新步骤会插入在当前选中步骤的下方

**方法二：使用按钮**

1. 当视频播放到关键步骤时，点击播放器下方的 **"⏱ Insert Timestamp"**（插入时间戳）按钮
2. 效果与按 `I` 键相同

**方法三：手动添加**

1. 点击右侧编辑器中的 **"Add Step"**（添加步骤）按钮
2. 手动输入时间戳和描述

#### 3.4 输入步骤描述

1. 插入时间戳后，在步骤的描述输入框中
2. 输入这个步骤的说明文字
   - 例如："导入素材到编辑软件"
   - 例如："调整视频播放速度到 0.5 倍"
   - 例如："添加转场效果"

#### 3.5 继续标注

1. 输入完描述后，按 **空格键** 继续播放视频
2. 重复步骤 3.3 和 3.4，标注所有关键步骤
3. 直到视频结束或所有重要步骤都已标注

#### 3.6 自动保存

编辑完成后，系统会**自动保存**：

- 编辑操作后 **1.5 秒** 自动保存（防抖设计，避免频繁保存）
- 切换视频时，如有未保存更改会**立即保存**
- 编辑器右上角会显示保存状态：
  - **● Unsaved**（橙色）：有未保存的更改
  - **⏳ Saving...**（蓝色）：正在保存
  - **✓ Saved**（绿色）：已保存
  - **✕ Save failed**（红色）：保存失败

> 💡 **提示**：您也可以随时点击 **"Save"** 按钮手动保存。

---

### 步骤 4：编辑和调整标注

#### 4.1 验证时间戳

1. 在右侧的步骤列表中，点击任意步骤的 **时间戳**（如 `00:11.230`）
2. 视频会自动跳转到该时间点并暂停
3. 检查时间戳是否准确，如果不准确可以手动修改

#### 4.2 修改时间戳

1. 直接点击时间戳输入框
2. 修改时间格式为：`mm:ss.SSS`（分钟:秒.毫秒）
   - 例如：`00:11.230` 表示 11 秒 230 毫秒
   - 例如：`01:23.456` 表示 1 分 23 秒 456 毫秒
3. 修改后，可以点击时间戳验证位置

#### 4.3 修改步骤描述

1. 直接点击描述输入框
2. 修改文字内容
3. 修改后会自动触发自动保存

#### 4.4 删除步骤

1. 找到要删除的步骤
2. 点击步骤右上角的 **`×`** 按钮
3. 步骤会被删除，其他步骤的编号会自动更新

#### 4.5 调整步骤顺序

1. 找到要移动的步骤
2. 点击步骤左侧的拖拽手柄（`⋮⋮` 图标）
3. 按住鼠标左键，拖动到目标位置
4. 松开鼠标，步骤会移动到新位置
5. 步骤编号会自动重新排序

#### 4.6 删除标注

如果需要删除已保存的标注并从预标注重新开始：

1. 点击编辑器右上角的 **"Delete"**（删除）按钮
2. 确认删除
3. 如果有预标注文件，会自动重新加载

---

### 步骤 5：保存标注

#### 5.1 检查标注内容

虽然有自动保存功能，保存前建议检查：

- ✅ 教程标题已填写
- ✅ 所有步骤的时间戳准确
- ✅ 所有步骤的描述清晰完整
- ✅ 步骤顺序正确

#### 5.2 标记非教程视频

如果这个视频不是教程视频，可以：

1. 勾选右侧编辑器底部的 **"Non-tutorial"**（非教程）复选框
2. 这样保存的文件会包含 `[not tutorial]` 标记

#### 5.3 手动保存

1. 点击右上角的 **"Save"**（保存）按钮
2. 系统会验证标注格式
3. 如果格式正确，文件会保存到您配置的输出目录
4. 保存成功后，视频列表中的状态会更新为 **🟢 Annotated**

> 💡 **提示**：保存的文件名与视频文件名相同，扩展名为 `.txt`。

---

### 步骤 6：编辑预标注文件

如果您有 AI 生成的预标注文件，可以加载并编辑：

#### 6.1 加载预标注

1. 选择有预标注的视频（显示 🟡 Pre-annotated 标识）
2. 视频加载后，预标注内容会自动显示在右侧编辑器
3. 包括标题和所有步骤

#### 6.2 验证和修正

1. 逐个点击步骤的时间戳，验证准确性
2. 修正不准确的时间戳
3. 修改或完善步骤描述
4. 删除多余的步骤
5. 添加缺失的步骤

#### 6.3 保存修正结果

1. 完成所有修正后，系统会自动保存
2. 也可以点击 **"Save"** 按钮手动保存

---

## 高级功能

### 视频播放控制

#### 播放速度调整

1. 在视频播放器下方，找到速度下拉菜单
2. 选择播放速度：
   - `0.5x`：慢速播放，适合精确标注
   - `0.75x`：稍慢，适合仔细观看
   - `1x`：正常速度
   - `1.25x`、`1.5x`、`2x`：快速播放，适合快速浏览

> 💡 **推荐**：
> - 首次观看：使用 `1x` 正常速度
> - 精确标注：使用 `0.75x` 或 `0.5x` 慢速
> - 快速浏览：使用 `1.5x` 或 `2x` 快速

#### 全屏播放

1. 双击视频播放器，或点击全屏按钮
2. 按 `Esc` 键退出全屏

#### 音量控制

1. 点击播放器上的音量图标
2. 拖动滑块调整音量

### 键盘快捷键

mp4Label 支持多种键盘快捷键，提高操作效率：

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `空格键` | 播放/暂停 | 在视频播放时暂停，暂停时播放 |
| `←` 左箭头 | 后退 0.5 秒 | 精确调整到前一帧 |
| `→` 右箭头 | 前进 0.5 秒 | 精确调整到后一帧 |
| `I` | 插入时间戳 | 在当前播放位置插入新步骤 |

> ⚠️ **注意**：快捷键在输入框中不会生效，这是为了避免误操作。需要先点击输入框外的区域。

### 复制时间戳

1. 在视频播放器下方，会显示当前播放时间
2. 点击这个时间显示
3. 时间戳会自动复制到剪贴板
4. 显示 "Copied!" 提示

### 模型标注对比（算法工程师专用）

如果配置了模型标注目录，界面会显示第四个面板：

1. 选择视频后，右侧最末端的面板会显示模型生成的标注（灰色背景）
2. 模型标注**只读**，不能编辑
3. 点击模型标注中的**时间戳**可以跳转视频到对应位置
4. 点击模型标注中的**描述文字**可以复制内容
5. 可以双击描述文字选择部分内容复制

**用途**：
- 对比模型标注与人工标注的差异
- 评估模型标注质量
- 发现模型标注的系统性错误

### 面板大小调整

界面中各面板之间有**可拖动的分隔条**：

1. 将鼠标移到面板边缘，光标会变为左右箭头
2. 按住鼠标左键拖动，可以调整面板宽度
3. 适合在不同显示器分辨率下使用

---

## 只读查看模式

### 什么时候使用

- ✅ 标注完成后，需要**安全地查看**而不怕误修改
- ✅ 给项目经理或客户**展示**标注成果
- ✅ QA 团队进行**质量审查**
- ✅ 不希望让使用者修改配置文件

### 启动方式

使用 `view` 子命令启动只读模式，所有配置通过命令行参数指定：

```bash
# 基本用法
./mp4label view -video-dir /path/to/videos -output-dir /path/to/annotations

# 完整参数
./mp4label view -port 9090 \
  -video-dir /path/to/videos \
  -output-dir /path/to/annotations \
  -pre-annotation-dir /path/to/pre-annotations \
  -task-file /path/to/task.txt \
  -task-dir /path/to/tasks \
  -model-annotation-dir /path/to/model-annotations
```

### 只读模式下的表现

启动后，界面上会有**明显的标识**：

- 🔴 页面标题旁显示红色 **"View Only"** 徽章
- 🔒 编辑器状态显示 **"🔒 View Only"**

以下操作会被**完全禁用**：

| 操作 | 状态 |
|------|------|
| 保存标注 | ❌ 按钮禁用，灰色显示 |
| 删除标注 | ❌ 按钮禁用 |
| 添加步骤 | ❌ 按钮禁用 |
| 插入时间戳 | ❌ 按钮禁用，`I` 键无效 |
| 编辑标题/描述/时间戳 | ❌ 输入框只读 |
| 拖拽排序步骤 | ❌ 拖拽手柄隐藏 |
| 删除步骤 | ❌ × 按钮隐藏 |
| 自动保存 | ❌ 完全关闭 |
| 修改配置 | ❌ 配置框内所有输入只读，保存按钮隐藏 |

以下操作**仍然可用**：

| 操作 | 状态 |
|------|------|
| 视频播放/暂停/快进/快退 | ✅ 正常 |
| 调节播放速度 | ✅ 正常 |
| 浏览视频列表 | ✅ 正常 |
| 搜索和过滤视频 | ✅ 正常 |
| 切换任务组 | ✅ 正常 |
| 查看标注内容 | ✅ 正常 |
| 点击时间戳跳转 | ✅ 正常 |
| 复制时间戳 | ✅ 正常 |
| 查看模型标注 | ✅ 正常 |
| 查看配置（只读） | ✅ 正常 |

### 参数说明

| 参数 | 必填 | 说明 |
|------|------|------|
| `-video-dir` | ✅ 是 | 视频文件目录 |
| `-output-dir` | ✅ 是 | 标注文件输出目录 |
| `-port` | 否 | 服务端口，默认 8080 |
| `-pre-annotation-dir` | 否 | 预标注文件目录 |
| `-task-file` | 否 | 单个任务文件路径 |
| `-task-dir` | 否 | 任务目录路径（包含多个 .txt 任务文件） |
| `-model-annotation-dir` | 否 | 模型标注文件目录 |

> 💡 **提示**：只读模式下**不会读取也不会写入** `~/.mp4label/config.json`，所有配置完全由命令行参数决定。

---

## 任务目录与任务组

### 使用场景

当您有大量视频需要多人协作标注时，可以将视频按组分配：

- 📝 每个标注员有自己的任务文件（如 `alice.txt`、`bob.txt`）
- 📂 所有任务文件放在同一个目录下
- 🔄 用户通过界面上的下拉框切换自己的任务组

### 配置方式

#### 方式一：通过配置对话框

1. 点击 **"Config"** 按钮
2. 在 **"Task Directory"** 输入框中输入任务目录路径
3. 点击 **"Save"**

#### 方式二：通过命令行

```bash
# 编辑模式
./mp4label web -task-dir /path/to/tasks

# 只读模式
./mp4label view -video-dir /videos -output-dir /output -task-dir /path/to/tasks
```

### 任务文件格式

每个 `.txt` 文件中，每行一个视频名（不含 `.mp4` 扩展名）：

```
video_001
video_002
tutorial_intro
advanced_effects
```

### 界面操作

配置任务目录后，侧边栏顶部会出现一个**醒目的蓝色下拉选择框**：

```
┌──────────────────────────────┐
│ Task Group (42 videos):      │
│ ┌──────────────────────────┐ │
│ │ annotator_alice.txt    ▼ │ │
│ └──────────────────────────┘ │
│ ┌──────────────────────────┐ │
│ │ 🔍 Search videos...      │ │
│ └──────────────────────────┘ │
│ Total: 42  Annotated: 15     │
│ Pre: 10    Unannotated: 17   │
│ [All] [None] [Pre] [Done]    │
│ ─────────────────────────── │
│ #0001 video_001.mp4 🟢      │
│ #0002 video_002.mp4 🟡      │
│ ...                          │
└──────────────────────────────┘
```

#### 切换任务组

1. 点击下拉框
2. 选择其他任务文件（如 `annotator_bob.txt`）
3. 视频列表和统计信息会**立即刷新**
4. 之前的编辑会在切换前**自动保存**

#### 查看全部视频

1. 在下拉框中选择 **"📋 All Videos"**
2. 会显示目录中的**所有视频**，不做任何过滤
3. 标签会变为 `Task Group (All - 150 videos):` 以示区别

### 优先级规则

| 配置情况 | 行为 |
|----------|------|
| 只设了 Task File | 单一过滤，不显示下拉框 |
| 只设了 Task Directory | 显示下拉框，可切换任务组 |
| 两个都设了 | **Task Directory 优先**，终端打印 WARNING 警告 |
| 都没设 | 显示所有视频，不显示下拉框 |

---

## 常见问题

### 问题 1：程序无法启动

**可能原因：**
- 文件权限问题（macOS/Linux）
- 端口 8080 被占用

**解决方法：**

**macOS/Linux 权限问题：**
```bash
chmod +x mp4label
```

**端口被占用：**
```bash
# 使用其他端口
./mp4label web -port 3000
```

### 问题 2：视频列表为空

**可能原因：**
- 视频目录路径错误
- 目录中没有 `.mp4` 文件
- 目录权限不足
- 任务文件中的视频名与实际文件名不匹配

**解决方法：**
1. 检查配置中的视频目录路径是否正确
2. 确认目录中确实有 `.mp4` 文件
3. 检查目录是否有读取权限
4. 如果使用了任务文件，检查文件内容是否与视频名一致

### 问题 3：视频无法播放

**可能原因：**
- 视频文件损坏
- 视频格式不支持
- 浏览器不支持该视频编码

**解决方法：**
1. 尝试用其他视频播放器打开，确认文件正常
2. 确保视频是 MP4 格式
3. 尝试使用其他浏览器（Chrome、Firefox）

### 问题 4：标注无法保存

**可能原因：**
- 输出目录路径错误或不存在
- 输出目录没有写入权限
- 标注格式不完整（缺少标题、时间戳格式错误、描述为空）
- 处于只读模式

**解决方法：**
1. 检查配置中的输出目录路径
2. 确认输出目录存在
3. 检查时间戳格式（`mm:ss.SSS`）和描述是否填写
4. 确认不是通过 `view` 命令启动的

### 问题 5：快捷键不工作

**可能原因：**
- 光标在输入框中
- 浏览器焦点问题
- 处于只读模式（`I` 键被禁用）

**解决方法：**
1. 先点击输入框外的区域
2. 刷新浏览器页面（`Ctrl+F5` 或 `Cmd+Shift+R`）
3. 如果是只读模式，`I` 键不可用是正常的

### 问题 6：任务组下拉框没有出现

**可能原因：**
- 没有配置任务目录
- 任务目录路径错误
- 任务目录中没有 `.txt` 文件

**解决方法：**
1. 打开 Config 对话框，检查 "Task Directory" 是否已正确填写
2. 确认目录存在且包含 `.txt` 文件
3. 保存配置后刷新页面

### 问题 7：自动保存没有生效

**可能原因：**
- 标注数据不完整（缺少标题、步骤描述为空等）
- 处于只读模式
- 网络或服务器问题

**解决方法：**
1. 自动保存只在标注数据**完整有效**时触发（标题非空、每个步骤有描述和正确的时间戳）
2. 查看编辑器右上角的状态指示器
3. 如有问题，尝试手动点击 Save 按钮查看错误信息

### 问题 8：预标注文件未加载

**可能原因：**
- 预标注目录路径错误
- 预标注文件名与视频文件名不匹配

**解决方法：**
1. 检查配置中的预标注目录路径
2. 确保预标注文件名与视频文件名相同（扩展名不同）
   - 视频：`tutorial.mp4`
   - 预标注：`tutorial.txt`

---

## 标注文件格式说明

### 教程视频格式

保存的标注文件格式如下：

```
教程标题

1) 00:11.230 第一步的描述
2) 00:25.560 第二步的描述
3) 00:42.890 第三步的描述
```

**格式要求：**
- 第一行：教程标题（不能为空）
- 空行
- 每行一个步骤：`序号) 时间戳 描述`
- 时间戳格式：`mm:ss.SSS`（分钟:秒.毫秒）

### 非教程视频格式

如果勾选了 "Non-tutorial" 选项，文件内容为：

```
[not tutorial]
```

---

## 命令行参考

### 编辑模式（web）

```bash
./mp4label web [选项]

选项：
  -port string        服务端口（默认 8080）
  -task-dir string    任务目录，包含多个 .txt 任务文件（可选）
```

### 只读模式（view）

```bash
./mp4label view [选项]

必填选项：
  -video-dir string          视频目录
  -output-dir string         标注输出目录

可选选项：
  -port string               服务端口（默认 8080）
  -pre-annotation-dir string 预标注目录
  -task-file string          单个任务文件
  -task-dir string           任务目录（多个 .txt 任务文件）
  -model-annotation-dir string 模型标注目录
```

### 其他命令

```bash
./mp4label version    # 查看版本
./mp4label help       # 查看帮助
```

### 使用示例

```bash
# 基本编辑模式
./mp4label web

# 指定端口和任务目录
./mp4label web -port 3000 -task-dir /data/tasks

# 只读查看模式（基本）
./mp4label view -video-dir /data/videos -output-dir /data/output

# 只读查看模式（完整）
./mp4label view -port 9090 \
  -video-dir /data/videos \
  -output-dir /data/output \
  -pre-annotation-dir /data/pre \
  -task-dir /data/tasks \
  -model-annotation-dir /data/model
```

---

## 使用技巧

### 技巧 1：高效标注流程

1. **第一次观看**：以正常速度（1x）完整观看视频，了解整体内容
2. **第二次标注**：以慢速（0.75x 或 0.5x）播放，使用 `I` 键快速插入时间戳
3. **第三次验证**：逐个点击时间戳，验证准确性并完善描述

### 技巧 2：精确时间定位

1. 使用 `←` 和 `→` 箭头键进行 0.5 秒的微调
2. 结合播放速度调整，可以精确定位到关键帧

### 技巧 3：批量处理

1. 使用任务目录功能，将视频按组分配给不同人
2. 使用状态过滤，优先处理未标注的视频
3. 利用搜索功能，快速找到特定视频

### 技巧 4：预标注优化

1. 加载预标注后，先快速浏览所有步骤
2. 使用时间戳跳转功能，验证每个步骤的准确性
3. 只修正不准确的部分，提高效率

### 技巧 5：团队协作

1. 为每个标注员创建独立的任务文件（如 `alice.txt`）
2. 使用任务目录功能，所有人使用同一个程序
3. 标注员通过下拉框选择自己的任务组
4. 管理者使用只读模式（`view`）审查所有人的标注

---

## 总结

mp4Label 是一款功能完善的视频标注工具，通过本教程，您应该能够：

- ✅ 正确配置和启动工具（编辑模式和只读模式）
- ✅ 高效地标注视频教程
- ✅ 利用自动保存功能安心编辑
- ✅ 使用任务组管理多人协作
- ✅ 利用只读模式安全地审查标注
- ✅ 对比模型标注与人工标注
- ✅ 解决常见问题

如果您在使用过程中遇到其他问题，请参考程序的文档或联系技术支持。

**祝您使用愉快！** 🎬✨
